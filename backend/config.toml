[server]
host = "127.0.0.1"
port = 3000

[redis]
host = "localhost"
port = 6379
password = "123456"

jwt_secret = "your-secret-key"
database_url = "sqlite:data/users.db"

[harbor]
url = "http://localhost:8097"
username = "admin"
password = "Harbor12345"
staging_project = "staging-project"
production_project = "production-project"
# registry_endpoint_id = 1       # optional: Harbor registry endpoint ID for local instance
# registry_insecure = true       # optional: force insecure registry endpoint (auto-detected for http/localhost)
# webhook_secret = "change-me"   # optional: secret shared with Harbor webhook custom header X-Webhook-Secret
